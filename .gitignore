# Environment files — the Gemini API key lives ONLY in server/.env
.env
.env.local
*.env

# Dependencies
node_modules/

# SQLite data folder
server/data/

# Local API-call logs
server/logs/

# Build output
dist/
client/dist/
server/dist/
*.tsbuildinfo

# Coverage reports (regenerated by `npm run test:coverage`; CI uploads
# these as a downloadable artifact instead of committing them)
coverage/

# OS / editor noise
.DS_Store
*.log
server/test-results/
.env.save
